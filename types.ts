
export type User = {
    email: string;
    name: string;
    password: string;
    id: string;
}

export type Video = {
    title: string;
    thumbnail: string;
    description: string;
    duration: number;
    youtubeid: string;
    date: string;
    id: string;
    fav: boolean;
}