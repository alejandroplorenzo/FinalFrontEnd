import { FunctionComponent } from "preact";

type Props = {
    id: string;
    fav: boolean;
    userid: string;
}

const Fav: FunctionComponent<Props> = ({id, fav, userid}) => {
    return(
        <>
        </>
    );
};

export default Fav;